/**
 * Enum with multiple types of validation error.
 */
enum ApiErrorType {
    AuthError,
    RouteError,
    TalkError,
    UserError,
    ValidationError,
    VoteError
}

export default ApiErrorType;

