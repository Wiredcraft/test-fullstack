import { NormalizeResponseInterceptor } from './normalize-response.interceptor';

describe('NormalizeResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new NormalizeResponseInterceptor()).toBeDefined();
  });
});
