// Import External Dependencies
import { Fragment, useEffect, useState } from 'react';
import {
  Routes,
  Route,
  useLocation,
  useNavigate,
  Outlet,
} from 'react-router-dom';
import styles from './Newest.scss';

export function Newest(props) {

  return (
    <div>
      newest
    </div>
  );
}
export default Newest;
